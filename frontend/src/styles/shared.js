import { css } from 'lit';

export default css`
  h2 {
    font-weight: normal;
    font-size: 22px;
  }

  h3 {
    font-weight: normal;
    font-size: 18px;
    margin: 0;
    padding: 0;
  }

  tp-button tp-icon {
    margin-left: 10px;
    --tp-icon-height: 18px;
    --tp-icon-width: 18px;
    --tp-icon-color: var(--text);
  }
`;
